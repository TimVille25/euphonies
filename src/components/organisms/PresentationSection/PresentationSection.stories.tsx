import type { Meta, StoryObj } from '@storybook/react';
import PresentationSection from '.';

const meta: Meta<typeof PresentationSection> = { component: PresentationSection };
export default meta;

type Story = StoryObj<typeof PresentationSection>;

export const Default: Story = {};
