import type { Meta, StoryObj } from '@storybook/react';
import ConcertList from '.';

const meta: Meta<typeof ConcertList> = { component: ConcertList };
export default meta;

type Story = StoryObj<typeof ConcertList>;

export const Default: Story = {};
